import { LandingHeader } from "@/components/landing/landing-header";

/** Minimal landing preview — header only. */
export function LandingPage() {
  return (
    <div className="min-h-screen bg-[#F9FAFF] text-[#183278] antialiased">
      <LandingHeader />
    </div>
  );
}
