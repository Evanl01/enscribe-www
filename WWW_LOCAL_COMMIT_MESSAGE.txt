Replace feature accordion screenshots with code mockups

Show each landing feature as a scaled React mockup instead of static PNGs,
so the accordion visuals match the product UI and stay sharp at any size.

- Add mockups for AI scribe (ambient panel + SOAP note), EHR push, visit prep,
  and billing/coding (ICD/CPT + generated letters); extract shared ScribeNote
  and GenerateDocuments panels
- Extend AiScribeFeatureMockup with a composite layout; swap animated waveform
  for a deterministic seeded static bar chart
- Wire all four FEATURES entries to mockupKey; remove marketing_ambient-scribe.png
- Refine accordion layout (2/5 copy, 3/5 sticky visual), min-heights, and
  per-feature aspect ratios via FeatureMockup / FeatureMockupNatural
- Add globals.css animations for scribe note cursor blink and EHR push button
  emphasis; register Tailwind @source for landing components
