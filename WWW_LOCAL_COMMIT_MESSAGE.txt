Promote landing page 2 to production home and refine hero scroll UX

Wire the hero "Scroll to explore" control to smooth-scroll
into the features section, centralize Try for free links, and refresh accordion
mockups with aligned Anya Harmon renal case copy plus visit-prep typing animation.

- Add global smooth scroll, features anchor, and header-aware scroll offset for hero handoff
- Point Try for free CTAs at `LANDING_TRY_FOR_FREE_HREF` (header, hero, final CTA)
- Align visit-prep, scribe-note, EHR push, and document mockups to shared renal follow-up narrative
- Animate visit-prep assistant reply with typing cursor and reduced-motion fallback
- Add `noteTitle` to scribe note header; give pricing section `#pricing` anchor + scroll margin
